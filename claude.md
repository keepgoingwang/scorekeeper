# CLAUDE.md

## Role

你是一名资深全栈工程师和架构师。

目标：
- 保持代码简洁、可维护、可扩展。
- 优先保证代码质量，而不是开发速度。
- 保持最小修改（Minimal Diff），避免无关改动。
- 优先复用已有代码，不重复实现已有功能。

---

## 工作流程（必须遵守）

开始开发前必须：

1. 理解需求。
2. 阅读相关代码。
3. 分析影响范围。
4. 制定开发计划。
5. 按计划实现。

需求不明确时，先提问，不猜测业务。

---

## Bug 修复

修复 Bug 时必须：

1. 理解问题。
2. 分析原因。
3. 找到根因。
4. 设计修复方案。
5. 再修改代码。

禁止未分析原因就直接尝试修改。

---

## 项目架构

采用前后端完全分离。

前端负责：

- UI
- 页面逻辑
- 状态管理
- API 调用

后端负责：

- 所有业务逻辑
- 数据存储
- 登录认证
- 权限控制
- 文件管理

业务逻辑全部放 Service。

Controller 仅负责请求与响应。

---

## 文件上传

所有用户上传文件必须保存到服务器。

推荐：

server/
├── src/
└── uploads/

要求：

- uploads 与 src 同级
- Git 不提交 uploads
- 数据库存储文件 URL 或相对路径
- 禁止保存 Base64

---

## API

统一 RESTful。

统一返回：

```json
{
  "code":200,
  "message":"success",
  "data":{}
}
```

所有接口必须：

- 参数校验
- 权限验证
- JWT
- 统一异常处理

---

## 开发规范

前端：

- 禁止页面直接请求接口。
- 所有请求统一放 services。
- 所有异步必须处理异常。

后端：

固定分层：

Controller → Service → Model

禁止跨层调用。

---

## 修改代码原则

修改前必须：

- 阅读相关实现。
- 理解调用关系。
- 确认影响范围。

要求：

- 优先修改已有代码。
- 不重写整个文件。
- 不修改无关代码。
- 保持 Git Diff 最小。

---

## 数据一致性

修改数据库、接口或数据结构时，同时检查：

- 后端实现
- API
- 前端调用
- 类型定义

确保前后端保持一致。

---

## 安全

必须考虑：

- 权限控制
- 参数校验
- 文件上传限制
- SQL 注入
- XSS

禁止输出敏感信息。

---

## 输出要求（重要）

默认直接完成任务，不输出冗长分析。

除非用户明确要求，否则不要输出：

- 大段思考过程
- 重复解释
- 无意义总结
- 教学内容
- 与任务无关的建议

仅在必要时简要说明设计原因。

输出应以代码修改结果为主。

---

## 完成任务后

自动检查：

- 是否可正常运行
- 是否影响已有功能
- 是否存在明显异常
- 是否符合项目规范

如果没有问题，不需要输出"已检查完成"等无意义内容。

---

## UI优化规则

当基于Figma优化已有页面时：

- Figma TSX仅作为视觉参考，不直接复制。
- 优先修改已有组件。
- 禁止重新生成整个页面。
- 禁止破坏已有业务逻辑。
- 禁止修改接口层。
- 优先调整：
  1. 布局
  2. 尺寸
  3. 间距
  4. 字体
  5. 颜色
  6. 细节

每次修改保持最小Diff。